query MediaDetailsById($id:Int!) {
    Media(type: ANIME, id: $id) {
        id
        countryOfOrigin
        title {
            romaji
            english
            native
        }
        description
        averageScore
        seasonYear
        coverImage {
            extraLarge
            color
        }
        bannerImage
        genres
        episodes
    }
}